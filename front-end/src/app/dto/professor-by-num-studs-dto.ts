export class ProfessorByNumStudsDto {
  // ! used to indicate that the property will be initialized later on
  id!: number;
  profFirstName!: string;
  profLastName!: string;
  numStudents!: number;
}
